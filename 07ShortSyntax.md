
//Short Syntax
// Sintaxe Curta

const nome = 'diego';
const idade = 27;

const user = {
    name: name,
    iage: idade,
};

//Com Short Syntax
const user = {
    name,
    iage,
};

document.body.innerText = JSON.stringify(user);

